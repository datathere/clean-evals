"""Alembic migrations for clean-evals.

Run via ``clean-evals migrate`` (which delegates to ``alembic upgrade head``).
The Alembic config (``alembic.ini`` + ``env.py``) ships inside this package
so the wheel is self-contained.
"""
